# Add project specific ProGuard rules here.
# minifyEnabled is currently false (see app/build.gradle.kts), so this file has no effect yet;
# it exists so proguardFiles() has a target once minification is turned on.
