<?xml version="1.0" encoding="utf-8"?>
<!-- Backs the FileProvider used for camera-capture screenshots (see AndroidManifest.xml). -->
<paths>
    <cache-path name="camera_images" path="camera/" />
</paths>
